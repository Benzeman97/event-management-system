security:
  jwt:
    public-key: classpath:public.txt
  oauth:
    access-token-uri: 'http://177.0.0.1:9092/oauth2/token'
    client-id: event-managment-service
    client-secret: rzv9pBSHclLoEaFmZ81PtmT03wAmaJSXLkOwi8tI=
    grant-type: password

database:
  host: 'event-managment-db.cb6kiu4gsw4z.us-east-1.rds.amazonaws.com'
  port: 5432
  db-name: event_management_db

spring:
  datasource:
    url: 'jdbc:postgresql://${database.host}:${database.port}/${database.db-name}?createDatabaseIfNotExists=true&autoReconnect=true&allowPublicKeyRetrieval=true&useSSL=false'
    username: postgres
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      connection-test-query: SELECT 1
  sql:
    init:
      mode: always
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: update
  redis:
    host: 104.36.85.71
    port: 6969
    cache:
      cache-names: EVENTS
      redis:
        time-to-live: 300000

logging:
  file:
    name: '/home/ubuntu/logs/event-management-sevice-dev.log'
  level:
    root: info
